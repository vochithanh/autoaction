package chl.autoaction.plus

import chl.action.ActionCore
import sln.app.Context
import vlin.ext.runIf

// Used to move next from action [Expected]
open class ActionContinue(context: Context, params: List<String>) : ActionCore(context, params) {
}